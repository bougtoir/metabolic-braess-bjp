# NATURE_BBS_SKELETON — NOT PRODUCED (stop rule)

Per the prespecified stop rule (Phase-9 spec §36) and the WEAK GO verdict in
PHASE9_GO_NO_GO.md, Nature escalation is stopped: route-level matched-
environment occupancy history effects disappear, and the Phase-8B HG signal is
dominated by retrospective route-level static persistence rather than
prospective hysteresis.

What a future paper skeleton could honestly claim (if ever revisited with
nonlinear occupancy models or marked-individual data):

1. Population-level distributional inertia in BBS is real but primarily
   reflects static route structure + lag-1 abundance autocorrelation.
2. A robust within-route, matched-environment prior-state effect on
   *abundance* (median +0.36 log-count, CI>0 in 73% of 124 species) — state
   dependence at fixed environment, not spatial hysteresis.
3. Regional (state-level) aggregation retains history gain (median 0.27) —
   history signal may live at coarser scales than routes.
4. History-aware forecasts redistribute more slowly than env-only ones after
   climate anomalies (speed bias 0.82 vs 1.00).

Target claim that FAILED falsification: "Avian distributions retain spatial
history beyond current and lagged environment, producing hysteresis and
delayed redistribution after environmental change" — not supported at route
level by prospective forward-chaining.
