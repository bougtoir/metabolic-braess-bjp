# Quaternary variable map

Same variable mapping as `protocols/cenozoic_mammals/VARIABLE_MAP.md`, plus:

| Core metric | Addition |
|---|---|
| M6 time-aggregation | extended ladder 100/50/20 kyr + event bins; export effect_coarse − effect_fine at each step |
| bridge test | gradient Dinosaur → Cenozoic → Quaternary → Modern of sampling bias, temporal resolution, trophic contrast, distance-decay stability (spec §21) |

Export target: `results/common_metrics/common_metrics_quaternary.csv`.
