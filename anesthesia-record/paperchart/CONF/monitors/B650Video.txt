// B650Video.txt
// B650 外部モニタ UVC+OCR paperChart モジュール設定

// @から始まる行は本モジュール専用設定
// Python実行ファイル（PATH通っている場合は python）
@Python=python

// 実行するPythonモジュール
@Module=anesthesia_record.monitor_video

// モニタ用ROI/ソース設定ファイル（@WorkingDir からの相対パス）
@Config=paperchart\b650_video.yaml

// UVCデバイス番号（既定は0）
@Device=0

// 取得間隔（秒）
@Interval=1.0

// paperChart への送信間隔（秒）。既定値は 300（5 分）で、時刻がこの秒数で割り切れるタイミングで送信される。
// 初回は即時送信。
@SendIntervalSec=300

// Python作業ディレクトリ（B650Video.exe 配置場所からの相対パス）
@WorkingDir=..\..\anesthesia-record

// デバッグウィンドウを表示して送るデータを確認する（True/False）
@Debug=False

// NIBP 3値の送信順（sdm タイプ時）。sys=収縮期、dia=拡張期、map=平均血圧。
// paperChart の表示に合わせて変更可（例: sys,dia,map / map,sys,dia / dia,map,sys）。
@NIBPOrder=sys,dia,map

// PpcCtrl 用システム設定
Startup=Hidden
SuppressZero=True

// バイタル項目の paperChart 項目名・単位マッピング
// 末尾にカンマ区切りで項目ごとの更新間隔（秒）を追加可能
// 省略時は @SendIntervalSec の値が使われる
// ソース名|SDM で 収縮期/拡張期/平均の3セットを @NIBPOrder で指定した順で送信（既定は sys,dia/map ＝ 大/小（中））
// ソース名|SD  で 収縮期/拡張期 の2セット
// spo2_waveform から派生する PI/PVI は "spo2_waveform.pi" 等で指定可
hr=HR,bpm
spo2=SpO2,%
nibp|sdm=NIBP,mmHg
bis=BIS,
st=ST,
spo2_waveform.pi=PI,%
spo2_waveform.pvi=PVI,%
